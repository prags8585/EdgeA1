# Copy to .env and fill in. Never commit .env or the shared Nano credentials.
HF_TOKEN=
JEV_API_KEY=                 # TypeSafe API key: https://docs.typesafe.ai
# JEV_TIMEOUT_S=2.0          # slower than this, the Nano decides alone
# REDIS_URL is added by scripts/serve_redis.sh (random password, 127.0.0.1 only)

CHECK_MODEL_PATH=models/check_model.joblib
CHECK_THRESHOLD=0.5
